components:
  terraform:
    # https://github.com/terraform-aws-modules/terraform-aws-s3-bucket
    # https://registry.terraform.io/modules/terraform-aws-modules/s3-bucket/aws/latest
    aws-s3-bucket/test:
      metadata:
        component: aws-s3-bucket
      vars:
        bucket: "{{ .atmos_stack }}-test"
